name: rulyotano.match - Publish

on:
  push:
    branches: 
      - main
    paths:
      - src/rulyotano.math/**

jobs:
  publish:
      uses: ./.github/workflows/npm-publish.yml
      with:
        path: ./src/rulyotano.math
      secrets: inherit
