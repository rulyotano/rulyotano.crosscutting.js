name: rulyotano.math.geometry - Build & Test 

on:
  pull_request:
    branches: 
      - main
    paths:
      - src/rulyotano.math.geometry/**

jobs:
  publish:
      uses: ./.github/workflows/build-test.yml
      with:
        path: ./src/rulyotano.math.geometry